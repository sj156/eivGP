% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Study_1_Helper.R
\name{bounded_slice_update}
\alias{bounded_slice_update}
\title{Bounded slice sampler update}
\usage{
bounded_slice_update(
  x0,
  logf,
  w = 1,
  lower = -Inf,
  upper = Inf,
  max_steps_out = 50,
  max_iter = 200
)
}
\arguments{
\item{x0}{Numeric scalar, current state.}

\item{logf}{Function that returns the log-density at a point.}

\item{w}{Estimated typical slice width (default 1).}

\item{lower}{Lower bound (default \code{-Inf}).}

\item{upper}{Upper bound (default \code{Inf}).}

\item{max_steps_out}{Maximum number of stepping-out steps (default 50).}

\item{max_iter}{Maximum iterations for the shrinkage phase (default 200).}
}
\value{
A list with components \code{x} (new state) and \code{n_eval}
(number of log-density evaluations).
}
\description{
Performs a single slice sampling step with stepping-out and
shrinkage on a bounded interval. The slice is constructed
around the current point \code{x0} using the given log-density
function \code{logf}.
}
