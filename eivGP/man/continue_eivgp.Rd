% Generated by roxygen2: do not edit by hand
% Please edit documentation in create-eivGP.Rmd.
\name{continue_eivgp}
\alias{continue_eivgp}
\title{Extend the existing posterior chains}
\usage{
continue_eivgp(
  object,
  n_iter,
  parallel = NULL,
  n_cores = NULL,
  verbose = FALSE
)
}
\arguments{
\item{object}{An unmodified fit returned by \code{\link[=fit_eivgp]{fit_eivgp()}} in version 0.3.0,
or by a previous call to this function. Earlier sampler checkpoints are
incompatible and must be refitted.}

\item{n_iter}{Additional transitions per chain, not additional saved draws.
Every post-warm-up transition is retained; there is no thinning.}

\item{parallel}{Whether to run chains in parallel. NULL keeps the fit setting.}

\item{n_cores}{Worker count. NULL keeps the fit setting.}

\item{verbose}{Whether to print sampler progress.}
}
\value{
An updated \code{eivgp_fit}, containing both old and new retained draws.
}
\description{
Extend the existing posterior chains
}
\details{
The terminal state, random-number stream, update schedule,
prior dictionary, and sampler settings are preserved. No new
warm-up is discarded and no model or sampler options can be changed here.
Checkpoints survive \code{saveRDS()}/\code{readRDS()}; use the same package version
and numerical environment for reproducible continuation. Older fits lacking
checkpoints must be refitted. Diagnostics in the returned fit are recomputed
over all retained draws; call \code{\link[=diagnose_eivgp]{diagnose_eivgp()}} again for target diagnostics.
Running longer is not a remedy for unidentified coordinates or chains
trapped in different modes. This is continuation of a completed call, not
periodic crash recovery during an interrupted fit.
}