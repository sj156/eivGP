% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Study_1_Figure.R, R/extract_imputation_df.R
\name{extract_imputation_df}
\alias{extract_imputation_df}
\title{Extract imputation summary data frame from fitted EIV-GP model}
\usage{
extract_imputation_df(fit, n_calib)

extract_imputation_df(fit, n_calib)
}
\arguments{
\item{fit}{A fitted EIV-GP model object, as returned by
\code{fit_eivgp_1d}.}

\item{n_calib}{Integer, the calibration set size used for this fit
(stored in the output as a metadata column).}

\item{n_calib_label}{Character, a label for the calibration size. If NULL, defaults to as.character(n_calib).}

\item{m}{Integer, number of latent classes. Defaults to config$m or 6L.}

\item{config}{A list of configuration parameters. Used to provide default values for m and other settings.
If not supplied, the function attempts to read from getOption("study1.config").}
}
\value{
A data.frame with columns: n_calib, n_calib_label, id, x, y, true_u, post_mean, post_lo, post_hi,
post_sd, width, error, abs_error, c (factor), covered.

A data frame with one row per missing observation and
columns: \code{n_calib}, \code{id} (original index), \code{true_u}, \code{post_mean},
\code{post_lo}, \code{post_hi}, \code{c} (ordinal class factor), \code{covered}
(whether the 95\% posterior interval covers \code{true_u}).
}
\description{
For an EIV-GP fit object and a given calibration size, computes
posterior summaries (mean, 2.5\%, 97.5\% quantiles) of the latent
variables \code{u} for observations that were \emph{not} part of the
calibration set.  The true (simulated) \code{u} values are also included.
}
\examples{
\dontrun{
  fit <- fit_eivgp_1d(train, test, ...)
  df <- extract_imputation_df(fit, n_calib = 10, m = 6)
}
}
