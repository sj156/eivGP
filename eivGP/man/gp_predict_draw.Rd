% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Study_1_Helper.R
\name{gp_predict_draw}
\alias{gp_predict_draw}
\title{GP predictive draw (1D)}
\usage{
gp_predict_draw(
  x_train,
  u_train,
  y_train,
  x_star,
  u_star,
  logtheta,
  sigma2_eps,
  noisy = FALSE
)
}
\arguments{
\item{x_train}{Numeric vector of training covariate \code{x}.}

\item{u_train}{Numeric vector of training latent variable \code{u}.}

\item{y_train}{Numeric vector of training responses.}

\item{x_star}{Numeric vector of test points for \code{x}.}

\item{u_star}{Numeric vector of test points for \code{u}.}

\item{logtheta}{Numeric vector of log-transformed GP hyperparameters.}

\item{sigma2_eps}{Error variance.}

\item{noisy}{Logical, if \code{TRUE} the predictive variance includes
\eqn{\sigma_\varepsilon^2} (default \code{FALSE}).}
}
\value{
A list with components \code{mean} (predictive mean vector) and
\code{var} (predictive variance vector).
}
\description{
Computes the predictive mean and variance for new inputs
\code{x_star}, \code{u_star} given training data. The predictive
distribution can be for the latent function (if \code{noisy = FALSE})
or for noisy observations (if \code{noisy = TRUE}, adding the error
variance).
}
