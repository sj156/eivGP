% Generated by roxygen2: do not edit by hand
% Please edit documentation in eivGP-literate-source.Rmd.
\name{impute_eivgp}
\alias{impute_eivgp}
\title{Posterior inference for latent continuous inputs}
\usage{
impute_eivgp(
  object,
  rows = NULL,
  new_C = NULL,
  draw_ids = NULL,
  n_per_draw = 1L,
  scale = c("auto", "raw", "model"),
  latent_sampler = c("minimax_tilting", "rejection", "gibbs"),
  n_new_latent_gibbs = 100L,
  rejection_batch_size = NULL,
  rejection_max_batches = 1000L,
  seed = NULL,
  ...
)
}
\arguments{
\item{object}{An object returned by \code{\link[=fit_eivgp]{fit_eivgp()}}.}

\item{rows}{Optional training-row indices. Ignored only when \code{new_C} is
supplied, in which case it must be \code{NULL}.}

\item{new_C}{Optional prospective ordinal inputs.}

\item{draw_ids}{Saved posterior draws to use; \code{NULL} uses every draw.}

\item{n_per_draw}{Number of prospective latent draws per posterior draw.
Training draws already contain one latent state per posterior draw and
therefore require \code{n_per_draw=1}.}

\item{scale}{Latent-coordinate scale. \code{"auto"} returns the physical raw
scale when calibration anchors it and the identified working/model scale
otherwise. \code{"raw"} is unavailable without a full-affine-rank calibration
design.}

\item{latent_sampler}{Prospective sampler for the multivariate ordinal-probit
engine. The default is a minimax-tilted accept--reject sampler that is exact
on successful completion. Exact prior rejection is an alternative, and
finite Gibbs is a diagnostic approximation.}

\item{n_new_latent_gibbs}{Number of sweeps for the optional Gibbs diagnostic.}

\item{rejection_batch_size, rejection_max_batches}{Exact-rejection controls.}

\item{seed}{Optional simulation seed for prospective units.}

\item{...}{Controls for prospective multivariate latent simulation.}
}
\value{
A numeric array with dimensions posterior draw by unit by latent
coordinate. Training rows retain their requested row labels; prospective
units follow the row order of \code{new_C}. Attributes \code{source}, \code{scale}, and
\code{latent_scale_anchored} describe the returned coordinates.
}
\description{
Returns posterior draws of the latent input for either training rows or
prospective ordinal observations. The two cases are mutually exclusive:
omit \code{new_C} to recover training-row draws, or supply \code{new_C} for new units.
}