% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Study_1_Helper.R
\name{log_prior_logtheta}
\alias{log_prior_logtheta}
\title{Log-prior for GP hyperparameters}
\usage{
log_prior_logtheta(
  logtheta,
  mean = c(log(3), log(0.5), log(0.5)),
  sd = c(1.5, 1.5, 1.5),
  bounds = rbind(log_rho = c(log(0.05), log(100)), log_theta_x = c(log(1e-04), log(100)),
    log_theta_u = c(log(1e-04), log(100)))
)
}
\arguments{
\item{logtheta}{Numeric vector of length 3.}

\item{mean}{Prior mean vector (default from original code).}

\item{sd}{Prior standard deviation vector (default from original code).}

\item{bounds}{3x2 matrix of lower and upper bounds for each
log-parameter (default from original code).}
}
\value{
Log-prior value, or \code{-Inf} if outside bounds.
}
\description{
Evaluates the log-prior density for the three log-transformed GP
hyperparameters (log-rho, log-theta_x, log-theta_u). The prior is
a product of independent normal densities truncated to a bounded
region.
}
