% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Study_1_Figure.R, R/extract_imputation_df.R
\name{make_cc_function_slice_df}
\alias{make_cc_function_slice_df}
\title{Extract slice data for Complete‑case GP function surface}
\usage{
make_cc_function_slice_df(
  fit,
  x_slices = c(-1, 0, 1),
  u_grid = seq(-2.4, 2.4, length.out = 160),
  scenario = "active",
  label = "Complete-case GP"
)

make_cc_function_slice_df(
  fit,
  x_slices = c(-1, 0, 1),
  u_grid = seq(-2.4, 2.4, length.out = 160),
  scenario = "active",
  label = "Complete-case GP"
)
}
\arguments{
\item{fit}{EIV-GP fit object (must contain \code{data$calib_idx} with
at least 3 observations for a meaningful GP fit).}

\item{x_slices}{Numeric vector of \code{x} values (default \code{c(-1,0,1)}).}

\item{u_grid}{Numeric vector of \code{u} points for evaluation.}

\item{scenario}{Character, \code{"active"} or \code{"inactive"}.}

\item{label}{Character label for the method (default \code{"Complete-case GP"}).}

\item{f0_fn}{Function. True data‑generating function with signature
\code{function(x, u, scenario)}. Default is \code{f0_1d} (assumed to
be available in the calling environment).}
}
\value{
A \code{data.frame} with the same structure as
\code{make_eiv_function_slice_df}. If fewer than 3 calibration
observations are available, an empty data frame is returned.

A data frame with columns \code{x_raw}, \code{u}, \code{mean}, \code{lo}, \code{hi},
\code{truth}, \code{method}, and \code{x_slice}, or an empty data frame if
calibration data are insufficient.
}
\description{
This function fits a standard GP to the complete‑case data (calibration
subset) and evaluates the predictive mean and 95\% pointwise intervals
over a grid of latent \code{u} values at fixed \code{x} slices.

For an EIV-GP fit that used calibration observations, this fits a
standard GP \emph{only} to the calibrated data (using the true \code{u}
values) and computes predictive summaries for the latent function
along the same \code{x}-slices.  If fewer than 3 calibrated observations
are available, an empty data frame is returned.
}
\examples{
\dontrun{
  df <- make_cc_function_slice_df(fit, x_slices = c(-1, 0, 1), f0_fn = f0_1d)
}
}
