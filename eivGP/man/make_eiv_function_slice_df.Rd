% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Study_1_Figure.R, R/extract_imputation_df.R
\name{make_eiv_function_slice_df}
\alias{make_eiv_function_slice_df}
\title{Extract slice data for EIV-GP function surface}
\usage{
make_eiv_function_slice_df(
  fit,
  x_slices = c(-1, 0, 1),
  u_grid = seq(-2.4, 2.4, length.out = 160),
  draw_ids = NULL,
  scenario = "active",
  label = "EIV-GP"
)

make_eiv_function_slice_df(
  fit,
  x_slices = c(-1, 0, 1),
  u_grid = seq(-2.4, 2.4, length.out = 160),
  draw_ids = NULL,
  scenario = "active",
  label = "EIV-GP"
)
}
\arguments{
\item{fit}{EIV-GP fit object.}

\item{x_slices}{Numeric vector of \code{x} values where slices are taken
(default \code{c(-1, 0, 1)} on the original scale).}

\item{u_grid}{Numeric vector of \code{u} values along which the
function is evaluated.}

\item{draw_ids}{Optional integer vector of posterior draw indices
to use.  If \code{NULL}, all saved draws are considered.}

\item{scenario}{Character, \code{"active"} or \code{"inactive"}, used to
compute the true function for comparison.}

\item{label}{Character string used as the method label in the output.}

\item{max_draw}{Integer or \code{NULL}. Maximum number of draws to retain.
If the number of selected draws exceeds this value, a random subset is taken.
Default is \code{1000L}.}

\item{include_process_uncertainty}{Logical. If \code{TRUE}, adds draw‑level
Gaussian noise from the predictive variance; otherwise uses only the mean.
Default is \code{TRUE}.}

\item{f0_fn}{Function. True data‑generating function with signature
\code{function(x, u, scenario)}. Default is \code{f0_1d} (assumed to
be available in the calling environment).}
}
\value{
A \code{data.frame} with columns:
\item{x_raw}{original predictor value}
\item{u}{latent grid value}
\item{mean}{posterior mean of the function}
\item{lo}{2.5\% posterior quantile}
\item{hi}{97.5\% posterior quantile}
\item{width}{= \code{hi - lo}}
\item{truth}{true function value from \code{f0_fn}}
\item{error}{= \code{mean - truth}}
\item{method}{method label}
\item{x_slice}{factor of \code{x_raw} with labels \code{"x = ..."}}

A data frame with columns \code{x_raw}, \code{u}, \code{mean} (posterior
mean of \code{f}), \code{lo} (2.5\% quantile), \code{hi} (97.5\% quantile),
\code{truth} (true \code{f} value), \code{method}, and \code{x_slice} (factor).
}
\description{
This function computes posterior mean and credible intervals for the
EIV-GP predictive function over a grid of latent \code{u} values at fixed
predictor \code{x} slices. It can optionally include process uncertainty
via Monte Carlo sampling.

Given an EIV-GP fit, computes posterior predictive summaries of
the latent regression function \code{f(x, u)} along several fixed-\code{x}
slices over a dense grid of \code{u}.  Uses a subset of posterior draws
(controlled by the global variable \code{n_pred_draw} when \code{draw_ids}
is not supplied).
}
\note{
This function expects the global variable \code{n_pred_draw} to be
defined when \code{draw_ids} is not provided.
}
\examples{
\dontrun{
  df <- make_eiv_function_slice_df(fit, x_slices = c(-1, 0, 1),
                                   max_draw = 500L, f0_fn = f0_1d)
}
}
