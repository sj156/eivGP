% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Study_1_Figure.R, R/extract_imputation_df.R
\name{make_long_draws}
\alias{make_long_draws}
\title{Reshape predictive draws to long format}
\usage{
make_long_draws(draw_mat, method, grid)

make_long_draws(draw_mat, method, grid)
}
\arguments{
\item{draw_mat}{Numeric matrix of draws (\code{n_draw} rows by
\code{nrow(grid)} columns).}

\item{method}{Character string identifying the method (stored in
the output column \code{method}).}

\item{grid}{A data frame with at least columns \code{x_star} and
\code{c_star}, having one row per test point.}
}
\value{
A data frame with columns \code{x_star}, \code{c_star}, \code{y} (draw),
and \code{method}.

A data frame with columns \code{x_star}, \code{c_star}, \code{y} (draw),
and \code{method}.
}
\description{
Converts a matrix of predictive draws (rows = draws, columns =
test points) into a long-format data frame suitable for plotting
with \code{ggplot2}.  Each row corresponds to one draw for one test
point, annotated with the corresponding covariates from \code{grid}.

Converts a matrix of predictive draws (rows = draws, columns =
test points) into a long-format data frame suitable for plotting
with \code{ggplot2}.  Each row corresponds to one draw for one test
point, annotated with the corresponding covariates from \code{grid}.
}
