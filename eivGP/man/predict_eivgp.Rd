% Generated by roxygen2: do not edit by hand
% Please edit documentation in eivGP-literate-source.Rmd.
\name{predict_eivgp}
\alias{predict_eivgp}
\title{Draw from an EIV-GP predictive distribution}
\usage{
predict_eivgp(
  object,
  new_X,
  new_C = NULL,
  new_U = NULL,
  new_U_scale = c("auto", "raw", "model"),
  target = c("response", "mean", "surface"),
  draw_ids = NULL,
  n_per_draw = 1L,
  joint = FALSE,
  include_process_uncertainty = TRUE,
  n_latent = 256L,
  latent_sampler = c("minimax_tilting", "rejection", "gibbs"),
  n_new_latent_gibbs = 100L,
  rejection_batch_size = NULL,
  rejection_max_batches = 1000L,
  seed = NULL,
  ...
)
}
\arguments{
\item{object}{An object returned by \code{\link[=fit_eivgp]{fit_eivgp()}}.}

\item{new_X}{Numeric prediction design matrix.}

\item{new_C}{Ordinal inputs for prediction of \code{Y} given \verb{(X,C)} or
inference on the observed-input mean \code{m(x,c)}. It may be omitted for
response prediction only when every row of \code{new_U} is fully observed.}

\item{new_U}{Optional exact continuous latent inputs. For response prediction,
complete rows condition on the supplied value and missing rows integrate
over the ordinal measurement model. It is required for \code{target="surface"}
and is not used for \code{target="mean"}.}

\item{new_U_scale}{Scale of supplied \code{new_U}. \code{"auto"} means the physical
raw scale when calibration anchors the latent coordinates and the model's
identified working scale otherwise. Requesting \code{"raw"} without a
full-affine-rank calibration design is an error.}

\item{target}{\code{"response"} for a future noisy response, \code{"mean"} for
\code{m(x,c)}, or \code{"surface"} for \code{f(x,u)}.}

\item{draw_ids}{Saved posterior draws to use; \code{NULL} uses every draw.}

\item{n_per_draw}{Number of predictive draws per posterior draw.}

\item{joint}{Whether each returned row is a coherent joint draw over all
evaluation points. The default is faster and gives correct pointwise
marginals, but \code{joint=TRUE} is required for surface realizations or
simultaneous functionals.}

\item{include_process_uncertainty}{Whether \code{mean} and \code{surface} draws include
conditional GP process uncertainty. When \code{FALSE}, conditional means are
Rao--Blackwellized within each posterior state.}

\item{n_latent}{Number of draws from \code{U} given \code{C} used to approximate the
integrals defining \code{m(x,c)} within each posterior state.}

\item{latent_sampler}{Prospective latent-input sampler for the multivariate
engine. The default is a minimax-tilted accept--reject sampler that is exact
on successful completion. Exact prior rejection is an alternative, and
finite Gibbs is an explicitly requested diagnostic approximation.}

\item{n_new_latent_gibbs}{Number of Gibbs sweeps when
\code{latent_sampler="gibbs"}.}

\item{rejection_batch_size, rejection_max_batches}{Controls for exact
prospective latent-input rejection sampling.}

\item{seed}{Optional predictive seed.}

\item{...}{Additional prediction controls.}
}
\value{
A numeric matrix with one row per retained posterior draw times
\code{n_per_draw} and one column per evaluation row. Attributes record the
estimand, whether columns form coherent joint draws, and (when applicable)
the latent-input scale and anchoring status. Row order follows \code{draw_ids},
with the \code{n_per_draw} replicates for each draw contiguous.
}
\description{
Draw from an EIV-GP predictive distribution
}