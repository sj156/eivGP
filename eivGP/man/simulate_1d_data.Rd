% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Study_1_Helper.R
\name{simulate_1d_data}
\alias{simulate_1d_data}
\title{Simulate 1D heterogeneous data}
\usage{
simulate_1d_data(
  n = 100,
  n_test = 500,
  m = 6,
  scenario = c("active", "inactive"),
  sigma_eps = 0.1,
  seed = NULL
)
}
\arguments{
\item{n}{Integer, number of training observations (default 100).}

\item{n_test}{Integer, number of test observations (default 500).}

\item{m}{Integer, number of latent classes (default 6).}

\item{scenario}{Character, \code{"active"} or \code{"inactive"}.}

\item{sigma_eps}{Numeric, residual standard deviation (default 0.1).}

\item{seed}{Optional integer for random seed.}
}
\value{
A list with components \code{train} (data frame), \code{test}
(data frame), \code{tau_true}, \code{sigma_eps}, \code{scenario}, and \code{m}.
}
\description{
Generates training and test data according to the one-dimensional
heterogeneous regression model. A Latin hypercube design is used for
the training covariate \code{x}; the latent variable \code{u} is drawn from a
standard normal and discretised into \code{m} classes using true
thresholds \code{tau_true}. The function repeatedly samples until every
class contains at least 3 training points.
}
