% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Study_1_Helper.R
\name{split_rhat}
\alias{split_rhat}
\title{Compute split-Rhat convergence diagnostic}
\usage{
split_rhat(chain_list)
}
\arguments{
\item{chain_list}{A list of numeric vectors, each representing
one MCMC chain (can contain \code{NA}/\code{Inf} values, which are removed).}
}
\value{
A single numeric value, the split-Rhat statistic, or
\code{NA_real_} if the diagnostic cannot be computed.
}
\description{
Splits each chain in \code{chain_list} into two halves, pools the
resulting sequences into a single matrix, and computes the
potential scale reduction factor (Rhat) using the split-chain
formula.  Only the most recent \verb{min(chain lengths)} iterations
are used to ensure equal lengths.  The function returns \code{NA}
if there are fewer than two chains, if the effective sample size
is too small, or if the within-chain variance is zero or invalid.
}
