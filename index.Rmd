---
title: "Creating the `eivGP` R package"
author: "eivGP authors"
date: "`r Sys.Date()`"
site: bookdown::bookdown_site
knit: litr::render
output: litr::litr_gitbook
params:
  package_name: "eivGP"
  package_parent_dir: "."
documentclass: book
---

# Literate package source

This book is the package-building skeleton for `eivGP`. The audited R modules
under `script/R-scripts/` are the computational source of truth. Each chapter
copies one coherent module into the generated package, so package code and the
paper's numerical implementation cannot drift apart.

The package book never launches publication-scale computation. Standalone
reproduction documents live under `experiments/` and `applications/`; they
call the same exported package functions used by scripts and downstream users.

Render and check the package from the repository root with:

```{r development-workflow, eval=FALSE}
litr::render("index.Rmd")
devtools::check("eivGP", document = FALSE)
```
