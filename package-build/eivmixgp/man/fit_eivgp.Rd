% Generated by roxygen2: do not edit by hand
% Please edit documentation in create-eivmixgp.Rmd.
\name{fit_eivgp}
\alias{fit_eivgp}
\title{Fit the EIV-GP for ordinal mixed inputs}
\usage{
fit_eivgp(
  X,
  y,
  C,
  U_obs = NULL,
  latent_dim = NULL,
  engine = NULL,
  m_vec = NULL,
  ordinal_levels = NULL,
  kernel = c("se", "matern"),
  matern_nu = 2.5,
  ident = NULL,
  standardize_U = NULL,
  store_scores = FALSE,
  parallel = TRUE,
  n_cores = NULL,
  ...
)
}
\arguments{
\item{X}{Numeric training design matrix.}

\item{y}{Numeric response vector.}

\item{C}{Ordinal inputs as factors or positive integer codes.}

\item{U_obs}{Optional calibrated latent inputs; unavailable rows are \code{NA}.}

\item{latent_dim}{Latent dimension. Inferred from informative \code{U_obs} when
possible. It must be supplied explicitly for the multivariate engine when
no calibrated latent input is available.}

\item{engine}{The scientific measurement model: \code{"univariate"} uses
deterministic thresholding of one latent input, whereas \code{"multivariate"}
uses the noisy ordinal-probit model. It must be selected explicitly.}

\item{m_vec}{Number of levels for each ordinal input.}

\item{ordinal_levels}{Optional ordered level labels, one vector per ordinal
input. Unordered factors require this argument.}

\item{kernel}{\code{"se"} or \code{"matern"}.}

\item{matern_nu}{Smoothness of the Matérn kernel.}

\item{ident}{Multivariate loading structure. The default is \code{"none"} for
full-affine-rank calibration and \code{"lower_triangular"} without calibration.
Sparse/rank-deficient calibration requires an explicit choice. Triangular
loadings together with diagonal ARD restrict the joint statistical model;
they are not merely a change of coordinates. Explicit \code{"none"} requires
at least one calibrated row in the supported public workflow.}

\item{standardize_U}{Whether calibrated latent inputs are standardized before
fitting. The default is \code{TRUE} when calibration is available.}

\item{store_scores}{Whether to retain ordinal-probit score draws.}

\item{parallel}{Whether to run independent MCMC chains in parallel.}

\item{n_cores}{Number of forked workers; \code{NULL} uses package settings.}

\item{...}{Additional sampler arguments such as \code{n_iter}, \code{burn}, and
\code{n_chains}. Every post-warm-up draw is retained: \code{thin}, if supplied, must
equal one. For the univariate engine, \code{noise_strategy = "collapsed"}
integrates out noise variance in the latent/kernel block; the default
\code{"conditional"} remains available as a same-posterior baseline.
For the multivariate interwoven sampler, optional
\code{control_overrides = list(joint_measurement_every = 2L, loading_transport_every = 5L)} enables joint loading/cut-point rows and
measurement-conditioned missing-input transport. Both default to zero;
measure target ESS/time before adopting them for a new application.}
}
\value{
An EIV-GP fitted object.
}
\description{
This is the stable package-facing interface for both the univariate and
multivariate ordinal-input samplers. Independent chains use forked R
processes on macOS/Linux and automatically fall back to serial execution.
}
\details{
Both engines use a fixed standard-normal latent population law,
independent of X, in model coordinates. Standardization uses calibrated
training rows and is plug-in preprocessing: its estimated moments are not
additional posterior parameters, and scaling does not remove correlations.
The multivariate score residual covariance is fixed to the identity.
These restrictions are recorded in \code{model_specification}. The affine-rank
flag used to permit raw-scale output is a conservative software reporting
policy; it neither proves statistical identification nor checks coverage.
}